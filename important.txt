cd ~/Desktop/Sports\ Square
git add .
git commit -m "Describe your changes"
git push origin main


<script async src="https://www.googletagmanager.com/gtag/js?id=G-6CR81XMZN5"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-6CR81XMZN5');
</script>

<script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-7385129367264824"
     crossorigin="anonymous"></script>

Add the code above to every page


<div id="premiumContent" style="display: none;">
  <h2>Premium Sports Breakdown 🏀</h2>
  <p>This exclusive analysis is available only to Sports Square members. Thanks for supporting us!</p>
</div>
const premiumContent = document.getElementById('premiumContent');

firebase.auth().onAuthStateChanged(user => {
  if (user) {
    premiumContent.style.display = 'block';
  } else {
    premiumContent.style.display = 'none';
  }
});



Article Template:

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>My New Article</title>
  <style>
    body {
      font-family: 'Montserrat', sans-serif;
      max-width: 800px;
      margin: auto;
      padding: 2rem;
      background-color: #f9fbf1;
      color: #333;
    }
    h1, h2 {
      color: #222;
    }
  </style>
</head>
<body>
  <h1>My New Article</h1>
  <p><em>By: Your Name</em></p>
  
  <p>Content goes here...</p>
  <p>More content...</p>
  <p>Keep scrolling until the bottom...</p>

  <script type="module">
    import { getFirestore, doc, setDoc, serverTimestamp, updateDoc, increment } from "https://www.gstatic.com/firebasejs/9.6.10/firebase-firestore.js";
    import { getAuth, onAuthStateChanged } from "https://www.gstatic.com/firebasejs/9.6.10/firebase-auth.js";

    const ARTICLE_ID = "my-new-article"; // ✅ Change for each article
    const ARTICLE_TITLE = "My New Article"; // ✅ Human-friendly name

    const auth = getAuth();
    const db = getFirestore();
    let hasScrolledToBottom = false;

    onAuthStateChanged(auth, (user) => {
      if (!user) return;

      window.addEventListener('scroll', async () => {
        if (hasScrolledToBottom) return;

        const scrollTop = window.scrollY;
        const docHeight = document.documentElement.scrollHeight;
        const winHeight = window.innerHeight;
        const scrollPercent = (scrollTop + winHeight) / docHeight;

        if (scrollPercent > 0.9) {
          hasScrolledToBottom = true;

          // Log user read
          await setDoc(doc(db, "reads", `${user.uid}-${ARTICLE_ID}`), {
            userId: user.uid,
            articleId: ARTICLE_ID,
            title: ARTICLE_TITLE,
            timestamp: serverTimestamp(),
            duration: 300 // Optional: Estimated read time in seconds
          });

          // Update global article read count
          const articleRef = doc(db, "articles", ARTICLE_ID);
          await setDoc(articleRef, { readCount: increment(1) }, { merge: true });
        }
      });
    });
  </script>
  <div id="rate-me" style="font-family: Arial, sans-serif; margin: 50px auto; text-align: center; padding: 20px; border: 2px solid #ccc; border-radius: 10px; background-color: #f9f9f9; max-width: 400px;">
    <label style="font-weight: bold;">Rate The Article:</label>
    <br>
    <div id="stars-container" class="stars" style="display: flex; justify-content: center; cursor: pointer; font-size: 3rem; color: lightgray; text-align: center;">
      <span class="star" data-value="1" style="margin-right: 10px;">&#9733;</span>
      <span class="star" data-value="2" style="margin-right: 10px;">&#9733;</span>
      <span class="star" data-value="3" style="margin-right: 10px;">&#9733;</span>
      <span class="star" data-value="4" style="margin-right: 10px;">&#9733;</span>
      <span class="star" data-value="5" style="margin-right: 10px;">&#9733;</span>
    </div>
    <button id="submit-rating" style="margin-top: 15px; padding: 6px 12px; font-size: 1rem;">Submit</button>
    <div id="feedback" style="margin-top: 10px; color: green; display: none;">Thank you for your rating!</div>
  </div>

  <script type="module">
    // Firebase setup
    import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-app.js";
    import { getFirestore, collection, addDoc } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-firestore.js";

    // Star rating logic
    let selectedRating = 0;
    const stars = document.querySelectorAll('.star');

    stars.forEach((star) => {
      star.addEventListener('mouseover', () => {
        const val = parseInt(star.getAttribute('data-value'));
        stars.forEach(s => {
          const sVal = parseInt(s.getAttribute('data-value'));
          s.style.color = sVal <= val ? 'gold' : 'lightgray';
        });
      });

      star.addEventListener('mouseleave', () => {
        stars.forEach(s => {
          const sVal = parseInt(s.getAttribute('data-value'));
          s.style.color = sVal <= selectedRating ? 'gold' : 'lightgray';
        });
      });

      star.addEventListener('click', () => {
        selectedRating = parseInt(star.getAttribute('data-value'));
        stars.forEach(s => {
          const sVal = parseInt(s.getAttribute('data-value'));
          s.style.color = sVal <= selectedRating ? 'gold' : 'lightgray';
        });
      });
    });

    document.getElementById('submit-rating').addEventListener('click', async () => {
      if (selectedRating === 0) {
        alert("Please select a rating before submitting.");
        return;
      }

      try {
        await addDoc(collection(db, "ratings"), {
          rating: selectedRating,
          timestamp: new Date()
        });

        document.getElementById("feedback").style.display = "block";
      } catch (e) {
        console.error("Error adding document: ", e);
        alert("Something went wrong. Check the console for details.");
      }
    });
  </script>
  
</body>
</html>
